# Local development defaults
DATABASE_URL=postgres://scanner:scanner@localhost:5432/scanner

# Optional host-map token. Public pk.* tokens are expected here.
MAPBOX_TOKEN=

# Optional server-side Shodan credential. Never prefix with NEXT_PUBLIC_ and
# never commit a real key. See README.md for the integration's data limits.
SHODAN_API_KEY=
